'use client'

/**
 * Inventory Filters Component
 */

export function InventoryFilters({ filters, onFiltersChange }: any) {
    return (
        <div className="text-center py-4 text-gray-500">
            Inventory filters component (stub)
        </div>
    )
}
