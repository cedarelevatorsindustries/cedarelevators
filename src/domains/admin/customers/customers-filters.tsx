'use client'

/**
 * Customers Filters Component
 */

export function CustomersFilters({ filters, onFiltersChange, onExport, isExporting }: any) {
    return (
        <div className="text-center py-4 text-gray-500">
            Customers filters component (stub)
        </div>
    )
}
